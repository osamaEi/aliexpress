APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# Admin email for ticket notifications
ADMIN_EMAIL=

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

# AliExpress API Configuration
# To get these credentials:
# 1. Register at https://openservice.aliexpress.com/
# 2. Create an app to get API_KEY and API_SECRET
# 3. Join dropshipping program at https://ds.aliexpress.com/
# 4. Generate ACCESS_TOKEN via OAuth2 authorization flow (see instructions below)
#
# OAuth2 Flow to get ACCESS_TOKEN:
# Automated flow using admin panel (RECOMMENDED):
# 1. Add your ALIEXPRESS_API_KEY and ALIEXPRESS_API_SECRET below
# 2. Visit: http://yoursite.com/admin/tokens
# 3. Click "Generate Access Token" button
# 4. Authorize on AliExpress and you'll be redirected back
# 5. Token will be automatically saved to database
#
# Manual OAuth2 Flow (Alternative):
# Step 1: Build authorization URL and visit it in browser:
#   https://api-sg.aliexpress.com/oauth/authorize?response_type=code&client_id=YOUR_API_KEY&redirect_uri=YOUR_REDIRECT_URI&state=random_string
#
# Step 2: After authorization, you'll be redirected to:
#   YOUR_REDIRECT_URI?code=AUTHORIZATION_CODE&state=random_string
#
# Step 3: Use the authorization code to get access token via API
#
# NOTE: The correct OAuth endpoint is https://api-sg.aliexpress.com/oauth/authorize
#       NOT https://oauth.aliexpress.com/authorize (this causes "appkey不存在" error)
#
ALIEXPRESS_API_KEY=
ALIEXPRESS_API_SECRET=
ALIEXPRESS_TRACKING_ID=
ALIEXPRESS_ACCESS_TOKEN=
ALIEXPRESS_API_URL=https://api-sg.aliexpress.com/sync

# PayPal Configuration
# To get these credentials:
# 1. Go to https://developer.paypal.com/
# 2. Log in with your PayPal account
# 3. Go to Dashboard > My Apps & Credentials
# 4. Create a new app (or use existing one)
# 5. Copy the Client ID and Secret from the app details
# 6. For testing, use Sandbox credentials
# 7. For production, switch PAYPAL_MODE to 'live' and use Live credentials
PAYPAL_MODE=sandbox
PAYPAL_SANDBOX_CLIENT_ID=
PAYPAL_SANDBOX_CLIENT_SECRET=
PAYPAL_LIVE_CLIENT_ID=
PAYPAL_LIVE_CLIENT_SECRET=
PAYPAL_CURRENCY=USD
PAYPAL_LOCALE=en_US
